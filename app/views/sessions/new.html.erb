<div class="row justify-content-center mt-4">
  <div class="col-md-6">
    <h3 class="pb-2 mb-4 border-bottom text-center"><%= t 'common.sign_in' %></h3>
    <%= render 'auth_providers' %>
    <% if AUTH_PROVIDERS.include?('password') %>
      <%= render 'form' %>
    <% end %>
  </div>
</div>
