<% cache [I18n.locale, comment, comment.user] do %>
  <div class="post">
    <div class="post-header">
      <div class="post-avatar">
        <%= avatar_tag comment.user %>
      </div>
      <span><%= comment.user.name %></span>
      <span><%= local_time_or_date comment.created_at %></span>
      <% if comment.edited_at %>
        <span>Edited at <%= local_time_or_date comment.edited_at %></span>
      <% end %>
    </div>
    <div class="post-body">
      <%= sanitize markdown_render comment.content %>
    </div>
    <div class="post-footer">
    </div>
  </div>
<% end %>
