<div class="list mt-3 mb-3" data-controller="infinite-page" data-infinite-page-page="<%= @notifications.current_page %>" data-infinite-page-reached-end="<%= @notifications.last_page? %>">
  <%= render partial: 'notification', collection: @notifications, cached: -> notification { [I18n.locale, notification] } %>
</div>
